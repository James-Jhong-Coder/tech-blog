---
const { class: className, ...rest } = Astro.props;
import Logo from '@/assets/icons/logo.svg';
import NavButton from '@components/NavButton.vue';
const currentPath = Astro.url.pathname;
console.log(currentPath);
---

<header class:list={['header-box', className]} {...rest}>
  <div class='content'>
    <div class='logo-box'>
      <Logo />
    </div>
    <div class='ml-auto flex items-center'>
      <NavButton is-active>首頁</NavButton>
      <NavButton class='ml-2'>文章列表</NavButton>
      <NavButton class='ml-2'>關於</NavButton>
      <NavButton class='ml-2'>聯絡</NavButton>
    </div>
  </div>
  <span class='bottom-line'></span>
</header>
<style>
  @reference "../styles/global.css";

  .header-box {
    @apply grid;
    grid-template-rows: 1fr 1px;
    grid-template-columns: 1fr;
    /* @apply flex flex-col; */
    /* border-bottom: 1px solid #e5e7eb; */
    /* @apply mx-auto px-3;
    @apply lg:px-0 lg:w-10/12; */
  }
  .content {
    @apply flex items-center;
    @apply px-3;
  }
  .logo-box {
    @apply flex items-center;
  }
  .logo {
  }
  .menus {
    @apply ml-auto;
  }
  .bottom-line {
    @apply h-px w-full;
    @apply bg-gray-100;
  }
</style>
