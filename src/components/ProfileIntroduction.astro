---
import ProfileCard from '@components/ProfileCard.astro';
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props as Props;
---

<ProfileCard class={`${className}`}>
  <div class='section text-white'>
    <div class='avatar'>
      <img
        src='https://shoplineimg.com/media/663c8aebb455500019558155/original.webp?source_format=jpg'
        class='rounded-3xl'
        alt=''
        srcset=''
      />
    </div>
    <!-- <div class='flex flex-col flex-1'> -->
    <div class='self-introduction'>
      <span class='text-md text-amber-300'>Hello 你好</span>
      <span class='mt-3 text-5xl'>我是 James</span>
      <span class='mt-6 leading-7'
        >我擔任前端工程師已經有六年的時間，熟悉 Vue
        生態系的開發，能打造穩定且易於維護的產品，會使用
        pinia、Vue2/3、Pinia、Vite、TypeScript、TailwindCSS</span
      >
    </div>
  </div>
</ProfileCard>

<style>
  @reference "@/styles/global.css";
  .section {
    @apply flex flex-col;
    @apply text-white;
  }
  .section .avatar {
    @apply shrink-0 flex-1;
  }
  .section .self-introduction {
    @apply flex flex-col;
    @apply mt-4;
  }
  @media screen and (min-width: 40rem) {
    .section {
      @apply flex-row-reverse;
    }
    .section .avatar {
      @apply ml-5 shrink-0 flex-1;
      flex-basis: 40%;
    }
    .section .self-introduction {
      flex-basis: 60%;
    }
  }
</style>
